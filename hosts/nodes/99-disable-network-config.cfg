# /etc/cloud/cloud.cfg.d/99-disable-network-config.cfg
#
# Stops cloud-init from writing network configuration on every boot. Without
# it, cloud-init owns /etc/netplan/50-cloud-init.yaml and can reassert what it
# decided during the install — which, for an offline install, was nothing.
network:
  config: disabled
