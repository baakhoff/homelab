# /etc/cloud/cloud.cfg.d/99-disable-network-config.cfg
#
# On the Debian 13 Pi OS image, cloud-init applies the imager's settings on
# first boot - and after an unclean power-off it ran again and removed the
# Wi-Fi profile it had created. NetworkManager owns the network from here on.
network: {config: disabled}
