# /etc/sysctl.d/99-tailscale.conf
#
# The Pi forwards packets for the tailnet: as an exit node (tailnet -> internet)
# and as a subnet router (tailnet -> LAN). Both need the kernel to route.
net.ipv4.ip_forward = 1
net.ipv6.conf.all.forwarding = 1
